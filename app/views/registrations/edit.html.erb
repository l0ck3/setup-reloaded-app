<h1>JUMP ON BOARD</h1>

<%= simple_form_for @student, url: registration_path(@student) do |f| %>
  <%= f.input :os, collection: Student::OS, as: :radio_buttons %>
  <%= f.button :submit, "I'm going on an adventure!", class: "button button-green" %>
<% end %>
